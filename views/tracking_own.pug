extends layout1

block content
    h1  #{title}
    ul.list-group
        each order, i in orders
            if users._id == order.username && order.paymentstatus == 0
                li.list-group-item
                    a(href="/payment/tracking/" + order._id)= order._id